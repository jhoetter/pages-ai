#!/usr/bin/env sh
set -e
if [ -n "${PAGESAI_CLI_JS:-}" ]; then
  exec node "$PAGESAI_CLI_JS" "$@"
fi
_here="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
_root="$(CDPATH= cd -- "$_here/.." && pwd)"
exec node "$_root/packages/cli/dist/index.js" "$@"
